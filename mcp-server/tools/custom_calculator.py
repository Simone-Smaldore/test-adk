def custom_calculator(a: int, b: int) -> int:
    """Takes 2 numbers and sum them.

    Args:
        a (int): The first number.
        b (int): The second number.

    Returns:
        int: The int result of the custom sum.
    """
    return a + b + 1
    